<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard Admin</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Admin Dashboard</a>
      <div class="d-flex">
        <a href="/admin/logout" class="btn btn-outline-light">Sair</a>
      </div>
    </div>
  </nav>

  <div class="container mt-5">
    <h2 class="mb-4">Projetos / Templates</h2>

    {{#if projects}}
      <div class="list-group">
        {{#each projects}}
          <div class="list-group-item d-flex justify-content-between align-items-center">
            <div>
              <strong>{{this.name}}</strong> - {{this.type}} - v{{this.version}}
            </div>
            <div>
              <!-- Botão para editar projeto -->
              <a href="/admin/projects/edit/{{this.id}}" class="btn btn-sm btn-primary me-2">Editar</a>

              <!-- Botão para acessar instâncias -->
              <a href="/admin/projects/{{this.id}}/instances" class="btn btn-sm btn-secondary me-2">Instâncias</a>


              <!-- Botão para ver tabelas do projeto -->
              <a href="/admin/tables?project_id={{this.id}}" class="btn btn-sm btn-success">Tabelas</a>
            </div>
          </div>
        {{/each}}

      </div>
    {{else}}
      <p>Nenhum projeto cadastrado ainda.</p>
    {{/if}}
  </div>
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Projetos / Templates</h2>
    <a href="/admin/projects/create" class="btn btn-success">+ Novo Projeto</a>
  </div>

</body>
</html>
